<section class="card">
    <a href="/subject/{{id}}">
        <header>{{title}}</header>
        <div class="card-body">
            <p>{{definition}}</p>
            <p>{{example}}</p>
        </div> 
    </a>
</section>
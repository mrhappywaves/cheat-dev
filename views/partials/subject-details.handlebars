<section class="card">
  <a href="/subject/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/public/images/{{filename}}" alt="{{description}}">
      <p>By: {{artist}}</p>
    </div>
  </a>
</section>